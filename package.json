{
  "name": "mmo-router",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn src/index.ts",
    "start": "tsc && node dist/index.js",
    "publish:patch": "tsc && npm version patch --no-git-tag-version && npm publish",
    "publish:minor": "tsc && npm version minor --no-git-tag-version && npm publish",
    "publish:major": "tsc && npm version major --no-git-tag-version && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pertinate/mmo-router.git"
  },
  "author": "Nicholas Evans",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/pertinate/mmo-router/issues"
  },
  "homepage": "https://github.com/pertinate/mmo-router#readme",
  "dependencies": {
    "@types/events": "^3.0.0",
    "@types/socket.io": "^2.1.10",
    "rimraf": "^3.0.2",
    "socket.io": "^2.3.0",
    "socket.io-redis": "^5.3.0",
    "ts-node": "^8.10.2",
    "ts-node-dev": "^1.0.0-pre.56",
    "typescript": "^3.9.7"
  },
  "devDependencies": {
    "@types/socket.io-redis": "^1.0.26"
  }
}
